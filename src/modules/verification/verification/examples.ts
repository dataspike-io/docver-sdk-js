import { SearchVerificationRequest } from './models';

const searchVerificationExample = {
  page: 0,
  limit: 10,
} as SearchVerificationRequest;


export const verificationExamples = {
  searchVerificationExample,
};
