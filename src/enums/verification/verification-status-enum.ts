export enum VerificationStatusEnum {
  INITIAL = 'initial',
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  VERIFIED = 'verified',
  FAILED = 'failed',
  EXPIRED = 'expired',
  CANCELED = 'canceled',
}
